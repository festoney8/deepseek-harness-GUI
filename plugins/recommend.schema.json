{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DSH Plugin List Schema",
  "type": "object",
  "required": ["plugins"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to the JSON Schema for this file"
    },
    "plugins": {
      "type": "array",
      "description": "DSH plugin list",
      "items": {
        "type": "object",
        "required": ["id", "name", "title", "description", "command", "github"],
        "properties": {
          "id": {
            "type": "string",
            "description": "plugin id, defined in cordis.patch.yml"
          },
          "name": {
            "type": "string",
            "description": "plugin name, defined in cordis.patch.yml"
          },
          "title": {
            "type": "string",
            "description": "plugin title"
          },
          "description": {
            "type": "string",
            "description": "plugin description"
          },
          "command": {
            "type": "string",
            "description": "plugin install command",
            "pattern": "^dsh plugin --profile web add "
          },
          "github": {
            "type": "string",
            "description": "GitHub repo url",
            "format": "uri",
            "pattern": "^https://github\\.com/"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
